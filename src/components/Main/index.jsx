//import Loading from "@/app/loading";

const Main = ({ children }) => {
    {/* h-[52.4rem] */ }
    return (
        <main className="relative h-full">
            {children}
        </main>
    )
}

export default Main;